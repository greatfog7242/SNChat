<Window x:Class="SNChat.App.Views.RenameConversationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rename Conversation"
        Height="180"
        Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Enter new conversation title:"
                   Margin="0,0,0,10"/>

        <TextBox Grid.Row="1"
                 x:Name="TitleTextBox"
                 Padding="8,6"
                 FontSize="13"
                 Margin="0,0,0,20"/>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="OK"
                    Click="OkButton_Click"
                    Padding="20,6"
                    Margin="0,0,8,0"
                    IsDefault="True"
                    MinWidth="75"/>
            <Button Content="Cancel"
                    Click="CancelButton_Click"
                    Padding="20,6"
                    IsCancel="True"
                    MinWidth="75"/>
        </StackPanel>
    </Grid>
</Window>
